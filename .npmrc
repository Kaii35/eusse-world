# Exponer las dependencias de forma estricta: si un paquete no la declara, no la ve.
# Evita dependencias fantasma que rompen al extraer un paquete.
shamefully-hoist=false
strict-peer-dependencies=false
auto-install-peers=true
engine-strict=true
